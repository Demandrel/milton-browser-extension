# Browser extension is intentionally OUTSIDE Milton's pnpm-workspace
# (which lists `milton` + `packages/*` only — see ../../pnpm-workspace.yaml).
# This file makes `pnpm install` / `pnpm build` / `pnpm test` self-contained
# so commands run from this directory don't fall through to the parent workspace.
ignore-workspace=true
