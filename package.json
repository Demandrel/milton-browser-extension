{
  "name": "milton-browser-extension",
  "version": "0.1.0",
  "description": "Save academic references to Milton from any browser tab.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.1.40",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.1.5"
  }
}
